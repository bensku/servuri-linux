zone {
  localhost
  public  *
}

# Restrict ingress
public-localhost {
  dhcp-client
  dhcpv6-client
  ping
  ssh
  udp 4242 # Nebula host
  udp 10000-11000 # Nebula containers
  drop log
}

# Allow all egress by default
localhost-public {
  accept
}
